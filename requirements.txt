# ==========================================================
# requirements.txt
# India Air Quality Analysis — ETL Pipeline Dependencies
# ==========================================================
# Install with:
#   pip install -r requirements.txt
# ==========================================================

# --- HTTP / API ---
requests==2.32.3          # OpenAQ API v3 calls
urllib3==2.2.3             # Retry/backoff adapter support (used by requests)

# --- Data Processing ---
pandas==2.2.3               # Data cleaning, transformation, CSV handling
numpy==2.1.3                # Numeric operations backing pandas

# --- Database ---
psycopg2-binary==2.9.9       # PostgreSQL driver
SQLAlchemy==2.0.36           # Efficient bulk loading with pandas.to_sql()

# --- Configuration / Secrets ---
python-dotenv==1.0.1          # Load credentials from .env (never hardcode)

# --- CLI / UX ---
tqdm==4.66.6                   # Progress bars for extraction & loading loops

# --- Logging (built-in, listed for clarity — no install needed) ---
# logging is part of the Python standard library

# --- Notebooks (optional, for exploratory analysis in /notebooks) ---
jupyter==1.1.1
ipykernel==6.29.5
matplotlib==3.9.2
seaborn==0.13.2
